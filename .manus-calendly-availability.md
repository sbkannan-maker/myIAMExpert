# Calendly Availability Validation

The embedded scheduler initialized correctly with the supplied URL, but Calendly returned: **“This calendar is currently unavailable.”** The direct URL is therefore reachable, but the event type does not presently expose bookable availability. The website should show a clear in-page fallback to LinkedIn/direct inquiry when Calendly does not emit its availability event.

The updated expert page now shows an in-page **Calendar availability is not published yet** fallback after the availability timeout. It exposes working **Send an inquiry**, **Connect on LinkedIn**, and **Retry calendar** actions instead of leaving an unusable scheduler frame.

On 20 August 2026, the expert page was rechecked after the recovery interval. The external Calendly iframe reported that the event calendar is unavailable, while the in-page recovery panel correctly displayed the direct inquiry address, **Copy** control, inquiry link, LinkedIn link, and retry action.

The copy action was exercised in the browser and changed to **Copied**, with the accessible confirmation **“Email copied to your clipboard.”** The Consulting page also showed topic filters for IGA Architecture, Identity Data Governance, SailPoint IdentityIQ, Microsoft Entra ID, Release Engineering, and Access Governance, including the on-site SailPoint IdentityIQ governed-delivery case study.

The SailPoint IdentityIQ filter was selected in the browser and correctly reduced the result grid to the Entra ID integration reference and the governed-delivery implementation case study. A fresh scheduler check also confirmed that the page initially communicates its loading state before showing the recovery panel when Calendly does not provide usable availability.

The finalized expert-page check reached the recovery state after the 6.5-second timeout and exposed the approved direct email plus Copy, inquiry, LinkedIn, and retry controls. The external Calendly iframe independently confirms that the configured calendar is currently unavailable.

The retry control was exercised successfully: it returned the panel to a visible loading state and then recovered a live Calendly **30 Minute Meeting** date-selection experience with available dates. This confirms the retry workflow can recover from a transient unavailable response.

Responsive screenshots at a 390px viewport confirmed that the Blog newsletter panel, Consulting case-study controls, and Expert scheduler section preserve readable spacing and usable stacked controls on mobile. The category-selection, copy-email, and retry handlers are client-side interactions shared by the responsive layouts; their behavior was exercised in the live browser.

True mobile emulation at a 390px viewport then exercised the touch controls directly. Selecting **SailPoint IdentityIQ** produced **“Showing 2 case studies for SailPoint IdentityIQ”** and retained the governed-delivery case card. On the Expert page, touch activation of **Copy** produced the clipboard confirmation, and touch activation of **Retry calendar** returned the panel to its visible availability-loading state; the inquiry and LinkedIn actions were both present and tappable.

On 20 August 2026, the configured URL `https://calendly.com/sbkannan/30min` was opened directly outside the website embed. Calendly itself returned **“This calendar is currently unavailable.”** The inline myIAM fallback is therefore responding to the current external event-type state, rather than an isolated website iframe failure. Publishing or re-enabling the 30-minute event type in the Calendly account is required before any inline widget can display bookable times.
