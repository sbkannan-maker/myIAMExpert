import { ArrowUpRight, BookOpenCheck, CalendarDays, CheckCircle2, Compass, FileLock2, GitPullRequest, Loader2, ShieldCheck } from "lucide-react";
import { useState } from "react";
import { useLocation } from "wouter";
import BrandLockup from "@/components/BrandLockup";
import ThemeToggle from "@/components/ThemeToggle";

const resources = [
  { icon: Compass, eyebrow: "Explore patterns", title: "Implementation reference catalogue", description: "Revisit the 42 IdentityIQ patterns and use the filters to map the conversation to a relevant delivery path.", path: "/", action: "Open use cases" },
  { icon: GitPullRequest, eyebrow: "Plan delivery", title: "CI/CD delivery guide", description: "Use the delivery guide to align architecture decisions with review gates, automation, and controlled releases.", path: "/delivery-guide", action: "Review delivery guide" },
  { icon: BookOpenCheck, eyebrow: "Build context", title: "IAM knowledge archive", description: "Continue with source-attributed technical notes and topic-filtered legacy implementation insights.", path: "/knowledge", action: "Browse knowledge" },
] as const;

export default function PostCallResources() {
  const [, setLocation] = useLocation();
  const [openingPath, setOpeningPath] = useState<string | null>(null);
  const navigateWithFeedback = (path: string) => { setOpeningPath(path); window.setTimeout(() => setLocation(path), 140); };
  return <div className="min-h-screen bg-[#060b18] text-slate-100"><header className="sticky top-0 z-40 border-b border-white/8 bg-[#07101e]/86 backdrop-blur-xl"><div className="container flex items-center justify-between gap-4 py-3"><button onClick={() => setLocation("/")} className="text-left"><BrandLockup size={34} /></button><ThemeToggle className="hidden sm:inline-flex" /></div></header><main className="container py-12 sm:py-18"><section className="max-w-3xl"><p className="flex items-center gap-2 font-mono text-[11px] font-semibold uppercase tracking-[.16em] text-cyan-300"><CheckCircle2 className="h-4 w-4" /> Continue after the call</p><h1 className="mt-5 text-4xl font-extrabold tracking-[-.055em] text-white sm:text-6xl">Keep the next step concrete.</h1><p className="mt-6 max-w-2xl text-base leading-7 text-slate-300">Use these resources to turn the architecture discussion into a controlled, evidence-led next step. Choose only what helps move the agreed decision forward.</p></section><section className="mt-11 grid gap-5 lg:grid-cols-3">{resources.map(({ icon: Icon, eyebrow, title, description, path, action }) => <article key={path} className="flex flex-col rounded-[1.6rem] border border-white/9 bg-[linear-gradient(145deg,rgba(21,33,59,.88),rgba(9,17,34,.88))] p-6 shadow-[0_24px_70px_-48px_rgba(35,146,225,.7)] transition duration-200 hover:-translate-y-1 hover:border-cyan-300/30 hover:shadow-[0_26px_76px_-44px_rgba(47,191,222,.82)]"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-cyan-300/10 text-cyan-200"><Icon className="h-5 w-5" /></span><p className="mt-6 font-mono text-[10px] font-semibold uppercase tracking-[.14em] text-cyan-300">{eyebrow}</p><h2 className="mt-2 text-xl font-bold tracking-[-.035em] text-white">{title}</h2><p className="mt-3 flex-1 text-sm leading-6 text-slate-400">{description}</p><button onClick={() => navigateWithFeedback(path)} className="mt-7 inline-flex items-center gap-2 text-sm font-bold text-cyan-300 transition hover:translate-x-1 hover:text-cyan-100">{openingPath === path ? <Loader2 className="h-4 w-4 animate-spin" /> : <ArrowUpRight className="h-4 w-4" />}{openingPath === path ? "Opening…" : action}</button></article>)}</section><section className="mt-6 flex flex-col gap-4 rounded-2xl border border-cyan-300/20 bg-[linear-gradient(120deg,rgba(34,211,238,.10),rgba(79,137,217,.08))] p-5 text-sm leading-6 text-slate-300 sm:flex-row sm:items-center sm:justify-between"><div><p className="flex items-center gap-2 font-mono text-[10px] font-semibold uppercase tracking-[.13em] text-cyan-200"><FileLock2 className="h-4 w-4" /> Approved client materials</p><p className="mt-2 max-w-2xl text-slate-400">Engagement-specific planning structures are available only after an approved client account signs in.</p></div><button onClick={() => navigateWithFeedback("/client-resources")} className="inline-flex shrink-0 items-center gap-2 rounded-xl border border-cyan-300/25 bg-cyan-300/10 px-3.5 py-2.5 font-bold text-cyan-100 transition hover:-translate-y-0.5 hover:bg-cyan-300/15">{openingPath === "/client-resources" ? <Loader2 className="h-4 w-4 animate-spin" /> : <FileLock2 className="h-4 w-4" />}{openingPath === "/client-resources" ? "Checking access…" : "Open client library"}</button></section><section className="mt-8 flex flex-col gap-4 rounded-2xl border border-cyan-300/15 bg-cyan-300/[.045] p-5 text-sm leading-6 text-slate-300 sm:flex-row sm:items-center sm:justify-between"><p className="flex gap-3"><ShieldCheck className="mt-0.5 h-4 w-4 shrink-0 text-cyan-300" /> Keep sensitive artefacts in the agreed secure channel; these resources are intended for safe, high-level planning context.</p><button onClick={() => navigateWithFeedback("/expert")} className="inline-flex shrink-0 items-center gap-2 font-bold text-cyan-300 transition hover:translate-x-1 hover:text-cyan-100">{openingPath === "/expert" ? <Loader2 className="h-4 w-4 animate-spin" /> : <CalendarDays className="h-4 w-4" />}{openingPath === "/expert" ? "Opening…" : "Book a follow-up"}</button></section></main></div>;
}
