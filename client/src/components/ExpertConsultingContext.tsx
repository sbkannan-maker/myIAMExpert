import { ArrowUpRight, BriefcaseBusiness, FileText, ShieldCheck } from "lucide-react";
import { useState } from "react";
import { consultingProfile } from "@/lib/consultingProfile";
import ProfessionalProfileCard from "@/components/ProfessionalProfileCard";

export default function ExpertConsultingContext() {
  const [caseStudyCategory, setCaseStudyCategory] = useState("All case studies");
  const categories = ["All case studies", ...Array.from(new Set(consultingProfile.caseStudies.flatMap(study => study.categories)))];
  const studies = caseStudyCategory === "All case studies" ? consultingProfile.caseStudies : consultingProfile.caseStudies.filter(study => (study.categories as readonly string[]).includes(caseStudyCategory));
  return <section className="border-t border-white/8 bg-[#060b18]"><div className="container py-14 md:py-20"><div className="grid gap-8 lg:grid-cols-[minmax(0,1fr)_20rem] lg:items-start"><div><p className="flex items-center gap-2 font-mono text-[11px] font-semibold uppercase tracking-[.16em] text-cyan-300"><BriefcaseBusiness className="h-4 w-4" /> Consulting context</p><h2 className="mt-4 max-w-2xl text-3xl font-extrabold tracking-[-.045em] text-white sm:text-4xl">Bring clarity to your identity program.</h2><p className="mt-4 max-w-2xl text-sm leading-7 text-slate-300">Discuss SailPoint IdentityIQ, identity governance architecture, secure integration patterns, and practical delivery approaches directly with Kannan. Select a booking slot above or use the inquiry form for a written starting point.</p><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{consultingProfile.consultationThemes.map((theme, index) => <article key={theme.title} className="rounded-2xl border border-white/9 bg-white/[.035] p-5"><div className="flex items-center gap-3"><span className="grid h-8 w-8 place-items-center rounded-lg bg-cyan-300/8 font-mono text-xs text-cyan-200">0{index + 1}</span><h3 className="text-sm font-bold text-white">{theme.title}</h3></div><p className="mt-4 text-sm leading-6 text-slate-400">{theme.description}</p></article>)}</div></div><div className="lg:pt-8"><ProfessionalProfileCard /></div></div><div className="mt-14 border-t border-white/8 pt-12"><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="font-mono text-[11px] font-semibold uppercase tracking-[.16em] text-cyan-300">Reference scenarios</p><h2 className="mt-3 text-2xl font-extrabold text-white sm:text-3xl">Architecture contexts & delivery patterns</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-400">Public references and non-client delivery scenarios, presented as planning context rather than unapproved customer claims.</p></div><a href={consultingProfile.linkedinUrl} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-1.5 text-sm font-bold text-cyan-300 hover:text-cyan-100">View all LinkedIn activity <ArrowUpRight className="h-4 w-4" /></a></div><div className="mt-7 flex flex-wrap gap-2" aria-label="Filter reference scenarios by topic">{categories.map(category => <button key={category} type="button" onClick={() => setCaseStudyCategory(category)} aria-pressed={caseStudyCategory === category} className={`rounded-full border px-3 py-1.5 text-xs font-semibold transition ${caseStudyCategory === category ? "border-cyan-300 bg-cyan-300 text-slate-950" : "border-white/12 bg-white/[.035] text-slate-300 hover:border-cyan-300/50"}`}>{category}</button>)}</div><p className="mt-4 text-xs text-slate-500" aria-live="polite">Showing {studies.length} {studies.length === 1 ? "reference" : "references"}{caseStudyCategory === "All case studies" ? "" : ` for ${caseStudyCategory}`}.</p><div className="mt-5 grid gap-4 md:grid-cols-2">{studies.map(study => <article key={study.id} className="rounded-2xl border border-white/9 bg-white/[.035] p-6 transition hover:-translate-y-0.5 hover:border-cyan-300/35"><div className="flex items-center justify-between gap-3"><span className="font-mono text-xs text-cyan-300">REFERENCE {study.id}</span><span className="rounded-full bg-cyan-300/8 px-2.5 py-1 text-[10px] font-semibold uppercase text-cyan-200">{study.type}</span></div><h3 className="mt-6 text-xl font-bold text-white">{study.title}</h3><p className="mt-3 text-sm leading-6 text-slate-400">{study.summary}</p><div className="mt-5 flex flex-wrap gap-2">{study.categories.map(category => <span key={category} className="rounded-md bg-cyan-300/8 px-2 py-1 text-[11px] font-medium text-cyan-200">{category}</span>)}{study.focus.map(item => <span key={item} className="rounded-md border border-white/10 px-2 py-1 text-[11px] text-slate-400">{item}</span>)}</div>{study.href ? <a href={study.href} target={study.href.startsWith("/") ? undefined : "_blank"} rel={study.href.startsWith("/") ? undefined : "noopener noreferrer"} className="mt-6 inline-flex items-center gap-1.5 text-sm font-bold text-cyan-300 hover:text-cyan-100">{study.href.startsWith("/") ? "Read the on-site case study" : "Read the public reference"} <ArrowUpRight className="h-4 w-4" /></a> : <p className="mt-6 inline-flex items-center gap-1.5 text-sm font-bold text-cyan-200"><FileText className="h-4 w-4" /> Reference scenario</p>}</article>)}</div></div><div className="mt-10 rounded-2xl border border-cyan-300/18 bg-cyan-300/[.045] p-5 text-sm leading-6 text-slate-300"><ShieldCheck className="mr-2 inline h-4 w-4 text-cyan-300" />The conversation is shaped around your operating context, implementation constraints, and governance evidence—not generic sales intake.</div></div></section>;
}
