import { Rss } from "lucide-react";
import { blogTopics, toBlogDiscoverySlug } from "@/lib/blogDiscovery";

export default function BlogTopicRssDirectory() {
  return <section className="border-t border-border/70 bg-secondary/25"><div className="container py-12 md:py-16"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="flex items-center gap-2 font-mono text-xs uppercase tracking-[.16em] text-accent"><Rss className="h-4 w-4" /> Topic subscriptions</p><h2 className="mt-3 text-2xl md:text-3xl">Subscribe to a specific identity topic.</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-muted-foreground">Choose an RSS feed for a focused topic. Open the feed in your reader to receive new matching articles as they are published.</p></div><a href="/rss.xml" target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 rounded-lg border border-primary/25 bg-primary/5 px-3.5 py-2.5 text-sm font-semibold text-primary hover:border-primary/50"><Rss className="h-4 w-4" /> All Blog articles</a></div><div className="mt-7 flex flex-wrap gap-2">{blogTopics.map(topic => <a key={topic} href={`/rss/topics/${toBlogDiscoverySlug(topic)}.xml`} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 rounded-full border border-border bg-card px-3 py-2 text-xs font-semibold text-muted-foreground transition hover:border-accent/55 hover:text-primary"><Rss className="h-3.5 w-3.5 text-accent" />{topic}</a>)}</div></div></section>;
}
